<% @motorbikes.each do |motorbike| %>
  <%= link_to motorbike_path(motorbike) do %>
    <h3>
      <%= motorbike.brand %> <%= motorbike.model %>
    </h3>
    <p><%= motorbike.description %></p>
  <% end %>
  <%= link_to 'Back to profile', user_path(motorbike.user) %>
<% end %>

